Essentiel
---------
x Avatars utilisateur — pas de photo de profil, le modele User n'a que id + display_name
x Scroll infini des messages — l'API supporte ?before=, mais le client ne charge que 50 messages sans "load more"
x Mot de passe — update_password existe en DB mais aucune route ni UI
x Ban — kick supprime le user, pas de systeme ban/unban
x Channel topic/description — aucun champ description sur les channels
x Icone/description du serveur — rien cote serveur info

Important
---------
x Reponses/quotes — pas de reply a un message
x Mentions (@user) — pas de parsing ni notification
x Reactions emoji — pas de reactions sur les messages
x Recherche de messages
x Unread par channel — le badge unread est global par serveur, pas par channel
x Systeme d'invitation — actuellement il faut le mot de passe serveur, pas de lien d'invite
x Detection des urls et preview (OG tags)
x Typing indicator — "X is typing..." via WebSocket
x Message pinning — epingler des messages importants

DMs — suite
-----------
- Attachments chiffres dans les DMs (cote client, meme modele que les messages)
- Rate limiting sur SendDm (anti-spam, serveur + client)
- Toast DM : reglage "masquer l'apercu du plaintext" dans les notifs

Nice to have
------------
x Markdown/formatage dans les messages (titre avec #, separator, text decoration, tableau, )
x Screen sharing — LiveKit le supporte, pas cable
x Move user entre channels voice (action admin)
x Limite d'utilisateurs par channel voice
x Audit log pour les actions admin
x Connexion rapide "TeamSpeak mode" — ip + mot de passe serveur, one shot, pas de compte
x DMs / messages prives — E2EE NaCl box, TOFU fingerprint, keypair per-server
x Webhooks — integrations (bots, CI notifications)
- Statut utilisateur (away/busy/DND) (maybe ... pas sur)
- MANAGE_NICKNAMES — permettre aux admins de changer le pseudo des autres
- Notifications desktop — notif push/desktop
- Themes — dark/light ou custom CSS
- Export de donnees — backup des messages pour l'admin
- pouvoir supprimé un guest user et donc tout ses messages aussi
- suppression automatique des guest users tout les X (parametrable)

Bugs
------------
x ws event pour les display name
x si je deaf et undeaf je n'entend plus rien, pas de undeaf ? pas de réabonnement Stream
x si j'entre dans un channel avec qqn qui stream, je ne le vois pas
x le state de je stream, je stream pas à l'air cassé ? si je quitte le vocal pendant que je stream
x indicateur pour qqn qui stream
x indicateur pour qqn webcam ?
x passé le state de navigation en session storage ?
x "reply to" fonctionne pas en prod ?
- soucis de notification, on recoit les notifs / le point reste meme si on est deja dans le channel

Awesome Features
----------------
- Stream depuis OBS
- custom emojis
- polls
- voice channel text chat
- forum channels
- Threads — reponses groupees sous un message
- Filtrage channels serveur-side — broadcasts cibles par user, per-user WS senders, recompute visibility sur changement de role/overwrite

Done
----
x Drag & drop fichiers dans le chat
x 2FA / TOTP — activer/desactiver par serveur, QR code, verification au login
x Rate limiting — 5 tentatives/min par IP sur /auth/login
x JWT TTL configurable — var d'env JWT_TTL_DAYS (defaut 5 jours)
x Token refresh automatique — renouvellement transparent avant expiration
x Separation onglets modal serveur — "Mon compte" vs "Serveur" dans la sidebar

Mise a jour / Deploy
--------------------
x PRAGMA foreign_keys = ON — jamais set, CASCADE inerte, donnees orphelines
x install.sh ecrase .env si relance — detecter "deja installe"
x ADMIN_PASSWORD reset a chaque restart si dans .env — utilise seulement a la creation
x Health check Docker — /health existe, pas configure dans compose
x Versioning images — toujours latest, pas de rollback possible
x Endpoint GET /version — pour que le client sache quelle version tourne
x Script update.sh — pull + up propre, separe de install.sh
